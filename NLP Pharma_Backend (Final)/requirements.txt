 How to Run
Save these files in the folder structure I showed.

Run MySQL locally and create the tables by executing schema.sql.

Update your DB credentials inside db_config.py.

Start the server:

bash
Copy
Edit
python app.py
In Dialogflow, set the webhook URL as:

bash
Copy
Edit
http://localhost:5000/webhook

Pharmabot SQL
Root
Il$a2142serv8r
